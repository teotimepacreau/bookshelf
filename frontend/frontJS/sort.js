const sorter = ()=>{
    const sorterBtn = document.querySelector('#sort-button')

    sorterBtn.addEventListener('click', ()=> {
        const sorterFields = document.createElement('selectlist')
    })
}

export default sorter